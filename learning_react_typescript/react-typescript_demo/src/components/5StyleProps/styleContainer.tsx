type ContainerProps = {
    styles: React.CSSProperties
}

export const ContainerStyle = (props: ContainerProps) => {
    return(
        <div style={props.styles}>
            
        </div>
    )
}